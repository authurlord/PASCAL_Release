# PASCAL — anonymous code release requirements
# Tested on Python 3.10 + CUDA 12.4; minimum Python 3.10.

# ─── Core agent runtime ───────────────────────────────────────────────
google-adk>=1.0.0
google-genai>=1.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# ─── Database backends ────────────────────────────────────────────────
psycopg2-binary>=2.9.9       # PostgreSQL (BIRD-Interact)
# sqlite3 ships with the CPython stdlib (PRACTIQ)

# ─── LLM client ───────────────────────────────────────────────────────
litellm>=1.40.0              # routes to vLLM OpenAI-compatible endpoint

# ─── SQL utilities ────────────────────────────────────────────────────
sqlglot>=23.0.0              # SQL segmentation for the user simulator

# ─── Tokenizer for context-window guard (optional) ────────────────────
transformers>=4.41.0         # used by shared/context_manager.py
sentencepiece>=0.2.0         # tokenizer dependency

# ─── Misc ─────────────────────────────────────────────────────────────
rich>=13.7.0
diskcache>=5.6.0             # Gemini response cache (user simulator)

# ─── LLM serving (separate install) ───────────────────────────────────
# vLLM has heavy CUDA build dependencies; install separately to match
# your platform. We require >= 0.19.1 for FP8 weights + chunked-prefill:
#   pip install "vllm>=0.19.1"
